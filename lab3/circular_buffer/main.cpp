#include "circural_buffer.h"

int main()
{
    // circular_buffer<int> buffer(1);

    // std::cout << "push_back";

    // buffer.push_back(1);
    // buffer.push_back(2);
    // buffer.out();
    // std::cout << buffer.size() << '\n';
    // buffer.push_back(3);
    // buffer.out();
    // std::cout << buffer.size()  << " " << buffer.capacity() << '\n';

    // std::cout << "push_front\n";

    // circular_buffer<int> gg(3);
    // gg.push_front(3);
    // std::cout << gg[0] << '\n';
    // gg.push_front(2);
    // std::cout << gg[0] << '\n';
    // gg.out();
    // std::cout << gg.back() << '\n';
    // gg.push_front(1);
    // gg.push_front(1);
    // gg.out();
    // std::cout << gg.size() << '\n';

    // std::cout << "pop_back and pop_front";

    // gg.pop_back();
    // gg.pop_front();
    // gg.out();
    // std::cout << gg.size() << '\n';

    // std::cout << "front: " << gg.front();

    // gg.resize(1);

    // std::cout << "\nback: " << gg.back();

    return 0;
}